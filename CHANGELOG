**v0.3.5**
- Add bruteforce module: hydra-based password cracking for SSH, FTP, HTTP, etc.
- Refactor: move wifi.ts from recon/ to access/ directory
- Refactor: move dirhunter.ts from enum/ to access/ directory
- Fix update nine.ts imports for moved modules

**v0.3.4**
- Add whois module for domain WHOIS lookup
- Add WHOIS details display in nine assets/show
- Fix remove mock data from nettree.ts
- Fix remove mock data from subfinder.ts
- Fix remove mock data from geoip.ts, use real geoip command
- Fix add missing asset displays in nine.ts (emails, directories, hashes, sessions, files)
- Fix python3 availability check after install in lib/python.ts

**v0.3.3**
- Add wifi cracking module
- Add cracked_wifi.txt persistence (loot/cracked_wifi.txt)
- Add skip already cracked networks
- Add pcap cleanup after crack

**v0.3.2**
- Improve pyUserEnum and dirhunter ui
- Fix pyEnumUser wasn't logging assets
- Fix pyUserEnum: scan all discovered IPs (not just first one)
- Fix runner: credential deduplication was broken (String(object) = "[object Object]")
- Fix runner: dedupe generic "pyUserEnum" source when specific "pyUserEnum on <ip>" exists
- Fix types: add ports field to NewAsset interface
- Fix runner: persist scanner ports in IP assets
- Fix nine show: add PORTS DETAILS section with port/service/version display
- Fix ui issues

**v0.3.1**
- Move subfinder to enum dir
- Fix nslookup parsing, improve ui
- Fix pyUserEnum: use interactive mode (output capture incompatible with script)
- Fix dirhunter: move temp file to ./tmp/ directory

**v0.3.0**
- Implement Milestone 3: Enumeration modules
- Add nslookup module: NS record lookup with deterministic IP fallback
- Add mxlookup module: MX record lookup for mail servers
- Add subfinder module: Subdomain enumeration with parent tracking
- Add lynx module: OSINT harvest for emails, IPs, and social handles
- Add pyUserEnum module: User enumeration via Python script wrapper
- Add dirhunter module: Directory bruteforce for web servers
- Update nine.ts with M3 module registry and command handlers

**v0.2.2**
- Add test for milestone-3

**v0.2.1**
- Add versionning to nine.ts
- Add list, history, assets, show mission to nine.ts
- Adjust rules to never use mock data in real module
- Fix nine list to return properly the mission

**v0.2.0**
- Fix: Scanner now properly creates IP assets for scanned targets (visible in nine status)
- Fix: Nettree module fixed Python script detection using ReadDir (stops re-downloading)
- Fix: Nettree color display using println with COLOR_PALETTE
- Refactor: Extract Python helper to lib/python.ts with ReadDir detection pattern
- Fix: Scanner wasn't logging scanned IP
- Fix: Nettree module wasn't downloading python module.

**v0.1.3**
- Update nine to match milestone 2
- Integrate recon modules: dig, geoip, nettree, scanner
- Add Python library integration
- Add module registry with aliases support

**v0.1.2**
- Add contexctual documentation for milestone 2
- Add test for milestone 2
- Remove version for refacto plan

**v0.1.1**
- Center the install ASCII
- Enhance nine ui

**v0.1.0**
- Mission-centric architecture refacto
- Core system: mission.ts, runner.ts, session.ts
- Mission commands: create, attach, detach, status
- Centralized UI library with color-coded output
- Type definitions for manifests and module results
- Storage abstraction layer

**v0.0.2**
- Fix netree module import issues
- Nine can now use -nt or --netree

**v0.0.1**
- Initial release
- IP scanner with subnet resolution
- Port status detection (open/forwarded/closed)
- Service version detection
- Color-coded terminal output
- Automatic report generation to ./loot/
- Global install via install.ts
