# Milestone 1 Context - Foundation

**Status**: Test validé (49/49 pass) → Prêt pour implémentation  
**Test file**: `tests/test-milestone-1.ts` (1150 lignes, version pragmatique)

---

## TypeScript Configuration

Tous les fichiers utilisant les APIs internes HackHub doivent commencer par `// @ts-nocheck` pour éviter les erreurs de lint :

```typescript
// @ts-nocheck
// File: lib/ui.ts, core/mission.ts, nine.ts, etc.
```

---

## Overview

Milestone 1 établit la fondation de Nine CLI : infrastructure, data model, et CLI dispatcher. 
Tout le reste du projet repose sur ces 8 fichiers.

---

## Fichiers à Implémenter (Ordre Critique)

### 1. lib/types.ts
**Dépendances**: Aucune  
**Exports**: Toutes les interfaces TypeScript

```typescript
export interface PortInfo { port: number; state: "open" | "closed" | "filtered" | "forwarded"; service: string; version?: string; forwarded?: { externalPort: number; internalPort: number; targetIp?: string }; }
export interface Seed { value: string; type: "ip" | "domain" | "email" | "cidr"; addedAt: string; resolvedIp?: string; }
export interface IPAsset { value: string; status: "discovered" | "scanned" | "exploited" | "pwned"; deviceType?: "router" | "firewall" | "printer" | "server" | "workstation" | "unknown"; ports: PortInfo[]; parent?: string; discoveredBy: string; discoveredAt: string; notes?: string; }
export interface DomainAsset { value: string; source: "seed" | "subfinder" | "lynx" | "dns"; parent?: string; resolvedIp?: string; vulnerable?: boolean; discoveredAt: string; }
export interface HistoryEntry { timestamp: string; module: string; target?: string; action: string; result: "success" | "failure" | "partial"; data?: unknown; }
export interface MissionManifest { name: string; created: string; updated: string; seeds: Seed[]; assets: { ips: IPAsset[]; domains: DomainAsset[]; emails: string[]; credentials: Array<{user: string; pass: string; source: string}>; hashes: string[]; ntlmHashes: Array<{ip: string; username: string; hash: string; cracked?: string; dumpedAt: string}>; sessions: Array<{type: "jwt" | "cookie" | "token" | "api_key"; value: string; source: string; target: string; extractedAt: string; decoded?: unknown}>; files: string[]; }; history: HistoryEntry[]; }
export interface ModuleMeta { name: string; command: string; description: string; requires: string[]; inputs: string[]; outputs: string[]; }
export interface ModuleResult { success: boolean; data?: unknown; newAssets?: Array<{ type: "ip" | "domain" | "email" | "credential" | "hash" | "session"; value: unknown; parent?: string }>; }
export type ModuleFunction = (mission: MissionManifest, ui: UI, args?: string[]) => Promise<ModuleResult>;
```

**Test validation**: `testStorage()` vérifie que les types fonctionnent avec JSON read/write.

---

### 2. lib/ui.ts
**Dépendances**: Aucune (mais utilise `println` global HackHub)  
**Exports**: `class UI`

**Palette couleurs obligatoire**:
```typescript
const COLOR_PALETTE = {
  white: "#ffffff", gray: "#6b7280",
  pink: "rgb(255, 0, 179)", cyan: "rgb(30, 191, 255)",
  green: "#22c55e", orange: "#f59e0b",
  red: "#ff4c4c", purple: "rgba(195, 105, 255, 0.86)",
  yellow: "#fbbf24"
} as const;
```

**Méthodes requises**:
- `UI.ctx()` - singleton pattern
- `info(msg)`, `success(msg)`, `warn(msg)`, `error(msg)`
- `section(title)`, `divider()`
- `print(label, value?, colors?)`
- `table(headers, rows)`

**Test validation**: `testUI()` vérifie que toutes les méthodes fonctionnent sans crash.

---

### 3. lib/storage.ts
**Dépendances**: `FileSystem` API (HackHub native)  
**Exports**: `readJson`, `writeJson`, `fileExists`, `deleteFile`, `ensureDir`

**Pattern critique - Paths absolues**:
```typescript
const cwd = await FileSystem.cwd();
const path = `${cwd.absolutePath}/loot/${mission}/manifest.json`;
```

**Test validation**: `testStorage()` vérifie read/write/delete JSON.

---

### 4. lib/utils.ts
**Dépendances**: Aucune  
**Exports**: `dedupeAssets`, `validateIp`, `sanitizeMissionName`, `getCurrentTimestamp`

**Logique clé - dedupeAssets**:
```typescript
function dedupeAssets<T extends { value: string }>(
  existing: T[],
  newAssets: Array<{ type: string; value: unknown; parent?: string }>
): { unique: Array<...>; duplicates: string[] }
```

**Test validation**: `testUtils()` vérifie dedupe avec duplicates et uniques.

---

### 5. core/mission.ts
**Dépendances**: `lib/types.ts`, `lib/storage.ts`, `lib/utils.ts`  
**Exports**: `MissionManager` class ou fonctions

**Fonctions requises**:
- `createManifest(missionName, seeds, cwdAbsolute)`
- `loadManifest(missionName, cwdAbsolute)`
- `saveManifest(manifest, cwdAbsolute)`

**Comportement**:
- Seed IPs → auto-ajout à `assets.ips` avec status "discovered"
- Seed domains → juste dans `seeds[]`
- `sanitizeMissionName()` pour nom de dossier safe

**Test validation**: `testMissionManager()` (8 tests dont cleanup).

---

### 6. core/session.ts
**Dépendances**: `lib/storage.ts`  
**Exports**: `getCurrentMission`, `setCurrentMission`, `clearCurrentMission`

**Fichier session**:
```
loot/.current_mission → {"mission": "Name", "attachedAt": "..."}
```

**Test validation**: `testSession()` (5 tests).

---

### 7. core/runner.ts
**Dépendances**: `lib/types.ts`, `lib/storage.ts`, `lib/utils.ts`  
**Exports**: `executeModule`

**RÈGLE CRITIQUE - Seul runner écrit le manifest**:
```typescript
async function executeModule(module, mission, cwdAbsolute, args?, ui?) {
  const result = await module.run(mission, ui, args);
  
  // 1. Dédoublonne newAssets
  const { unique, duplicates } = dedupeAssets(existing, result.newAssets);
  
  // 2. Ajoute au manifest
  for (const asset of unique) {
    mission.assets[asset.type + 's'].push(formattedAsset);
  }
  
  // 3. Log history
  mission.history.push({ module: module.meta.name, result: result.success ? "success" : "failure", ... });
  
  // 4. Persiste
  await saveManifest(mission, cwdAbsolute);
  
  return result;
}
```

**Test validation**: `testRunner()` (6 tests : execution, history, assets, dédoublonnage, failures).

---

### 8. nine.ts
**Dépendances**: Tous les fichiers précédents  
**Exports**: None (CLI entry point)

**Commandes requises**:
- `nine create <mission> [seeds...]`
- `nine attach <mission>`
- `nine detach`
- `nine status`
- `nine scan [ip]` (mock module pour test)
- `nine nettree [ip]` (mock module pour test)

**Pattern dispatch**:
```typescript
const MODULES: Record<string, { module: any; aliases: string[] }> = {
  scan: { module: scanner, aliases: ["-s", "--scan"] },
  nettree: { module: nettree, aliases: ["-n", "--nettree"] },
};
```

**Test validation**: `testCLI()` (7 tests du workflow complet).

---

## Dépendances Graph

```
lib/types.ts (0 deps)
    ↓
lib/ui.ts (0 deps - juste println global)
    ↓
lib/storage.ts (FileSystem API)
    ↓
lib/utils.ts (0 deps)
    ↓
core/mission.ts (types + storage + utils)
    ↓
core/session.ts (storage)
    ↓
core/runner.ts (types + storage + utils + mission)
    ↓
nine.ts (tout)
```

**Règle**: Implémenter dans cet ordre strict. Chaque fichier dépend des précédents.

---

## Validation Finale

**Test complet**:
```bash
node tests/test-milestone-1.ts full-test
# Expected: 49/49 PASS
```

**Checklist avant commit**:
- [ ] Tous les fichiers créés dans l'ordre
- [ ] Test passé sur HackHub (copier/coller)
- [ ] `/audit --full M1` → 10/10 points
- [ ] Pas de `Shell.Process.exec()` sauf wrappers Python
- [ ] Paths absolues partout
- [ ] UI class utilisée (pas println direct)
